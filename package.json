{
  "name": "wallet-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "for p in core signer verify-sdk protocol cli mcp-server; do echo \\\"=== $p ===\\\" && (cd packages/$p && bun run build) || exit 1; done",
    "test": "for p in core signer cli verify-sdk protocol mcp-server; do (cd packages/$p && bun test) || exit 1; done",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/nodemailer": "^8.0.1",
    "typescript": "^5.5.0"
  }
}
