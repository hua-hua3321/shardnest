{
  "name": "@wallet-services/protocol",
  "version": "0.3.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "bun run build",
    "test": "bun test"
  },
  "dependencies": {
    "@noble/curves": "^1.4.0",
    "@noble/hashes": "^1.4.0",
    "@wallet-services/core": "workspace:*",
    "@wallet-services/verify-sdk": "workspace:*"
  },
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ]
}
