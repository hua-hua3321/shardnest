# 2026-08-12 工作日志

## shardnest 钱包服务 · 三视角安全审查
以架构/安全/加密视角审查了 6 个包核心代码 + SECURITY.md + DES-016 + 协议 schema（145 用例）。
- 结论：无致命密码学缺陷；7 条不变式（生成元=3、EIP-191 三处一致、CRC 覆盖 index:hex、私钥范围、内存清零、地址交叉校验、原子性）均正确落地。
- **P0-1**：MCP 工具所有显式错误返回漏 `isError: true`（grep 确认仅抛异常路径有），LLM 可能误判拒绝为成功——直接削弱双闸门。须补 `isError:true`。
- **P0-2 / 架构**：单进程架构（MCP+signer 同进程）仍是最大结构风险，路线图 P0-3 未落地。
- **P1**：display 未纳入钱包签名（所见即所签缺口）；重放全委托平台；口令仅校验长度≥12；passphrase purpose 校验可被调用方绕过（缺省即跳过）。
- 报告产物：`SECURITY-REVIEW-2026-08-12.md`（仓库根）。
