# 2026-08-12 工作日志

## shardnest 钱包服务 · 三视角安全审查
以架构/安全/加密视角审查了 6 个包核心代码 + SECURITY.md + DES-016 + 协议 schema（145 用例）。
- 结论：无致命密码学缺陷；7 条不变式（生成元=3、EIP-191 三处一致、CRC 覆盖 index:hex、私钥范围、内存清零、地址交叉校验、原子性）均正确落地。
- **P0-1**：MCP 工具所有显式错误返回漏 `isError: true`（grep 确认仅抛异常路径有），LLM 可能误判拒绝为成功——直接削弱双闸门。须补 `isError:true`。
- **P0-2 / 架构**：单进程架构（MCP+signer 同进程）仍是最大结构风险，路线图 P0-3 未落地。
- **P1**：display 未纳入钱包签名（所见即所签缺口）；重放全委托平台；口令仅校验长度≥12；passphrase purpose 校验可被调用方绕过（缺省即跳过）。
- 报告产物：`SECURITY-REVIEW-2026-08-12.md`（仓库根）。

## shardnest 安全加固落地（续 · 安全工程师）
审查报告承诺的"未完成任务"已推进：
- **依赖漏洞扫描（OSV）**：解析 bun.lock 109 个版本，0 CVE。关键库均安全版本（noble ciphers 1.3.0 / curves 1.9.7 / hashes 1.8.0、scure bip32 2.3.0 / bip39 1.6.0、nodemailer 9.0.5、mcp-sdk 1.30.0、zod 3.25.76）。
- **P0-1（MCP isError）**：代码已落地 errResp()，23 处错误返回全带 isError:true；mcp.test 11 处断言通过。
- **P1-7（purpose 绑定）**：加固 `consumeUnlockSession`——对 passphrase 类型直接比较 `purpose !== data.purpose`（含 null 必须相等），封死"漏传 purpose"与"误建 null-purpose 会话"两窗口；unlock 类型不受影响。新增 2 条回归测试（unlock-session.test.ts）。
- **P0-3 设计**：新增 `docs/DES-017-isolated-signing-daemon.md`（三进程边界 + 认证 IPC + OS 原生批准 + Keychain/enclave + 沙箱 + 4 阶段迁移）。
- 验证：全量 147 测试通过（0 失败）；`tsc --noEmit` 通过。
- 未做（建议下一轮）：P1 #2 display 未签名 / #3 重放 / #4 口令强度；P2 邮件 TLS、令牌文件名前缀、CI 固化 osv 扫描。
- 关键文件：packages/signer/src/unlock-session.ts（purpose 加固）、packages/signer/test/unlock-session.test.ts（回归）、SECURITY-REVIEW-2026-08-12.md §7（进展）、docs/DES-017-isolated-signing-daemon.md（设计）。

## shardnest 安全加固 · 第二轮（P1-#3 重放 / P1-#4 口令熵）
- 核实：P1-#2（display 签名）已落地（walletSignMessage v3 含 display 域分离）；P1-#4 基础校验（字符类/重复/序列）此前已强化——审查报告相对当前代码多次过时。
- **P1-#3 重放**：新增 `packages/mcp-server/src/replay-guard.ts`（ReplayGuard 类），在 signed_request_sign 验签后、消费解锁令牌前拦截已用 nonce（key=platformAddress:nonce，惰性清过期）。新增测试 mcp.test.ts「P1-3 同 nonce 重放→NONCE_REUSED」。
- **P1-#4 口令熵**：validatePassphrase 加香农熵下限（≥30 bits，捕获 Aaaaaaaaaaaa1 类低熵）；新增 estimatePassphraseEntropy 辅助。scrypt SCRYPT_OPTS.N 2^17→2^18（256MB，旧钱包参数持久化故向后兼容）。commands.test.ts 新增熵拒绝测试；O1 测试断言同步改 2^18。
- 验证：全量 149 测试通过（0 失败）；tsc --noEmit 通过。
- 关键文件：replay-guard.ts（新）、mcp-server/src/index.ts（接入）、cli/src/commands.ts（熵）、core/src/keys.ts（scrypt）；SECURITY-REVIEW-2026-08-12.md §7.6–7.9（进展）。
- 剩余 P2：邮件 TLS、令牌文件名前缀改 sha256(token)、lagrange 逆元表、@ts-expect-error 封装、CI 固化 osv；P0-3 设计已完待落地。
- **用户决策（22:26）：P2 项与 P0-3 暂不实装**，除非后续主动要求。安全审计+加固范围已收敛完成。详见 MEMORY.md。
